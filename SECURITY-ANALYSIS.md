# KaoPass セキュリティ技術レポート

## 1. システム概要

KaoPassは、顔貌認証と表情パスワードを組み合わせた多層認証システムである。
従来の「顔認証」が顔の構造のみで認証するのに対し、KaoPassは**顔の構造 + 表情の順序 + 表情筋の個人パターン**の3要素を同時に検証する。

全処理はブラウザ内で完結し、生体データは端末内のIndexedDBにのみ保存される。

---

## 2. 使用技術

### 2.1 MediaPipe Face Landmarker
- **バージョン**: @mediapipe/tasks-vision@0.10.18
- **出力**: 478個の3D顔ランドマーク + 52種のブレンドシェイプスコア
- **ブレンドシェイプ**: 各表情筋の活性度を0.0〜1.0の連続値で出力（例: `mouthSmileLeft: 0.72`, `eyeBlinkRight: 0.45`）
- **用途**: 表情分類、頭部姿勢推定、Z深度アンチスプーフィング
- **読込方式**: ESM dynamic import（`vision_bundle.mjs`）、GPU優先/CPU fallback

### 2.2 face-api.js
- **バージョン**: @vladmandic/face-api@1.7.14
- **モデル**: SSD MobileNet v1（顔検出）+ 68点ランドマーク + FaceRecognitionNet
- **出力**: 128次元の顔特徴ベクトル（Float32Array）
- **用途**: 顔貌の個人識別、登録済み顔との照合
- **距離関数**: ユークリッド距離 → 類似度変換: `similarity = 1 - (distance / 1.2)`

### 2.3 IndexedDB
- **ストア**: faces（顔特徴ベクトル×5アングル）、expressions（表情シーケンス+ブレンドシェイププロファイル）、settings
- **暗号化**: ブラウザのSame-Origin Policyにより保護

---

## 3. 認証フロー

```
[顔認証フェーズ]
  カメラ映像 → MediaPipe(ランドマーク) → face-api.js(128次元ベクトル)
  → 登録済みベクトルとユークリッド距離比較
  → 類似度 > 0.6 で通過

[表情認証フェーズ]（30秒制限内）
  カメラ映像 → MediaPipe(ブレンドシェイプ52次元)
  ├── カテゴリ判定: どの表情か？（6択）
  ├── バイオメトリクス: 本人の表情パターンか？（コサイン類似度）
  └── 常時顔照合: 1.5秒ごとにface-api.jsで顔を再検証

  スコア = カテゴリ一致度 × 70% + バイオメトリクス類似度 × 30%
  → ターゲット表情が最高スコア + 30%以上 + 500ms安定 で1ステップ通過
  → 全ステップ通過で認証成功
```

---

## 4. セキュリティレイヤー分析

### 4.1 レイヤー1: 顔貌認証（128次元ベクトル）

**原理**: face-api.jsのFaceRecognitionNetが顔画像から128次元の特徴ベクトルを抽出する。このベクトルは顔の骨格構造、目鼻口の配置比率、顔の輪郭形状を数値化したもの。同一人物のベクトルはユークリッド距離が小さく、異なる人物のベクトルは距離が大きい。

**FAR（他人受入率）**: 閾値0.6で約0.003%（≈ 1/32,000）

**エントロピー**: log₂(32,000) ≈ **15 bit**

**常時再照合による強化**: 表情認証中も1.5秒間隔で顔を再照合する。これにより：
- 認証途中での別人への入れ替え攻撃を検出
- 表情による顔変形下でも持続的に本人確認
- 閾値は0.45（表情による変形を考慮し、初回認証の0.6より緩和）

### 4.2 レイヤー2: 表情カテゴリシーケンス

**原理**: MediaPipeの52種ブレンドシェイプから6種の表情を分類する。

| 表情 | 主要ブレンドシェイプ | 閾値 |
|---|---|---|
| 笑顔 | (mouthSmileLeft + mouthSmileRight) / 2 | > 0.3 |
| 口開け | jawOpen | > 0.3 |
| キス | mouthPucker | > 0.3 |
| 左ウィンク | eyeBlinkLeft - eyeBlinkRight | > 0.15 |
| 右ウィンク | eyeBlinkRight - eyeBlinkLeft | > 0.15 |
| 眉上げ | browInnerUp | > 0.25 |

**組み合わせ数**: 表情6種のN個の順列（重複あり）= 6^N

| ステップ数 | 組み合わせ | エントロピー |
|---|---|---|
| 3 | 216 | 7.8 bit |
| 4 | 1,296 | 10.3 bit |
| 5 | 7,776 | 12.9 bit |
| 6 | 46,656 | 15.6 bit |
| 8 | 1,679,616 | 20.7 bit |

### 4.3 レイヤー3: 表情バイオメトリクス（52次元ベクトル照合）

**原理**: 同じ「笑顔」でも、表情筋の使い方は人によって異なる。

- Aさん: mouthSmileLeft=0.72, mouthSmileRight=0.65（左が強い）
- Bさん: mouthSmileLeft=0.60, mouthSmileRight=0.71（右が強い）

登録時に52次元のブレンドシェイプベクトル全体を保存し、認証時にコサイン類似度で照合することで、「笑顔であること」だけでなく「**この人固有の笑顔であること**」を検証する。

**コサイン類似度**:
```
similarity = (A · B) / (|A| × |B|)
```
ここでA, Bは52次元ベクトル。同一人物の同じ表情では0.9以上、異なる人物では0.6〜0.8程度になると推定される。

**追加エントロピー推定**:
52次元のベクトル空間で個人を区別できるパターン数を推定する。

- 52次元のうち有効な独立次元: 推定10〜20次元（多くのブレンドシェイプは相関する）
- 各独立次元で区別可能な段階数: 推定2〜4段階
- 保守的推定: 2^3 = 8パターン/表情 → **+3 bit/step**
- 楽観的推定: 2^5 = 32パターン/表情 → **+5 bit/step**

---

## 5. 総合エントロピー計算

### 5.1 計算式

```
総エントロピー = 顔認証(15bit) + Σ[各ステップ(カテゴリ + バイオメトリクス)]
```

顔認証は1回の判定（ステップごとに独立ではない）。
各ステップのカテゴリ選択は独立。
バイオメトリクスは表情ごとに異なる筋肉パターンを使うため、ステップ間で部分的に独立。

### 5.2 8ステップの場合

**保守的推定（+3 bit/step）**:
```
1ステップあたり: 6種 × 8パターン = 48通り（5.58 bit）
8ステップ: 48^8 = 28,179,280,429,056 ≈ 2.82 × 10^13
× 顔認証 32,768
= 9.24 × 10^17
≈ 92京通り（59.7 bit）
```

**楽観的推定（+5 bit/step）**:
```
1ステップあたり: 6種 × 32パターン = 192通り（7.58 bit）
8ステップ: 192^8 = 1.85 × 10^18
× 顔認証 32,768
= 6.06 × 10^22
≈ 600垓通り（75.7 bit）
```

### 5.3 ステップ数別まとめ

| ステップ数 | 保守的（92京の場合8） | 楽観的（600垓の場合8） |
|---|---|---|
| 3 | 362万 (21.8bit) | 2.3億 (27.7bit) |
| 4 | 1.7億 (27.4bit) | 445億 (35.3bit) |
| 5 | 83億 (33.0bit) | 8.5兆 (42.9bit) |
| 6 | 4,000億 (38.5bit) | 1,640兆 (50.5bit) |
| 8 | **92京 (59.7bit)** | **600垓 (75.7bit)** |

### 5.4 他方式との比較

| 認証方式 | 通り数相当 | エントロピー |
|---|---|---|
| 4桁PIN | 1万 | 13.3 bit |
| Touch ID（指紋） | 5万 | 15.6 bit |
| Face ID（顔認証） | 100万 | 19.9 bit |
| 8文字英数パスワード | 218兆 | 47.6 bit |
| **KaoPass 8ステップ（保守的）** | **92京** | **59.7 bit** |
| **KaoPass 8ステップ（楽観的）** | **600垓** | **75.7 bit** |

---

## 6. なぜ「92京〜600垓通り相当」と言えるのか

### 6.1 掛け算が成立する条件

多層認証の組み合わせ数が掛け算になるためには、各レイヤーが**独立**である必要がある。

**独立性の根拠**:

1. **顔認証 × 表情カテゴリ**: 顔の骨格構造（128次元）と表情の種類（笑顔/口開け等）は独立した情報。顔が似ていても表情の順番は推測不可能。

2. **表情カテゴリ × バイオメトリクス**: 「どの表情を選んだか」（6択）と「その表情をどう表現するか」（52次元の筋肉パターン）は異なる次元の情報。

3. **ステップ間の独立性**: 各ステップは異なる表情を要求するため（例: ステップ1=笑顔、ステップ2=キス）、使う筋肉群が異なり、バイオメトリクスパターンが部分的に独立。

### 6.2 掛け算が完全には成立しない部分

1. **顔認証の常時再照合**: 同一人物の顔を繰り返し照合しても、結果は強く相関する（同じサイコロの目を何度も読む状態）。ただし表情変形による変動で+1〜2 bit/stepの上乗せはある。

2. **バイオメトリクスの推定幅**: +3〜5 bit/stepは理論推定であり、大規模な被験者テストで実測したものではない。正確な値の確定には100人以上の被験者データが必要。

### 6.3 確定値と推定値の区分

| 要素 | 値 | 根拠 | 確度 |
|---|---|---|---|
| 顔認証 15bit | 32,768通り | face-api.js FAR公表値ベース | 高 |
| 表情カテゴリ 6^N | 確定的計算 | 数学的に確定 | 確定 |
| バイオメトリクス +3〜5bit | 8〜32パターン/表情 | 52次元ベクトルの理論的分離度 | 推定 |
| **総合 59.7〜75.7bit** | **92京〜600垓** | **上記の積** | **推定範囲** |

---

## 7. 攻撃耐性

| 攻撃手法 | 対策 | 有効性 |
|---|---|---|
| 写真/画面表示 | Z深度検証（鼻-耳のZ差） | 高 |
| 動画再生 | Z深度 + 表情バイオメトリクス | 高 |
| 他人がなりすまし | 顔認証（128次元）で排除 | 高 |
| 顔認証後に入れ替え | 常時顔照合（1.5秒間隔） | 高 |
| 表情順序の盗み見 | バイオメトリクスで筋肉パターン不一致 | 中〜高 |
| ブルートフォース | 30秒タイムアウト + クールダウン | 高 |
| ディープフェイク | Z深度がフラット → 検出 | 中 |

---

## 8. 制約と今後の課題

1. **バイオメトリクスの実測検証**: 推定値（+3〜5 bit/step）を被験者テストで実測する必要がある
2. **環境依存性**: 照明条件、カメラ品質、角度によりFAR/FRRが変動する
3. **再現性**: 本人の表情再現性（日による変動）の定量評価が未実施
4. **WebカメラとFace IDの精度差**: Face IDは赤外線ドットプロジェクタ3万点、KaoPassはRGBカメラのみ
5. **パスワードマネージャー化**: 現在はデモ段階。Vault機能は後続フェーズ

---

*作成日: 2026-02-15*
*KaoPass v1.0 — https://mizuking796.github.io/kaopass/*
